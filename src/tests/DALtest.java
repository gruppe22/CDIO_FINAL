package tests;

import dao.BrugerDAO;
import dao.IBrugerDAO;
import dto.BrugerDTO;

import java.util.List;

public class DALtest {
/*


@Test
        public test
            BrugerDTO Kasper = new BrugerDTO(1, "Kasper", "KB", "2510882134", "Administrator");
            BrugerDTO Hans = new BrugerDTO(2, "Hans Henning", "HH", "1903995783", "Pharmaceut");
            BrugerDTO Stig = new BrugerDTO(3, "Stig the HAWK", "SHG", "1901565234", "Laborant");
            IBrugerDAO dao = new BrugerDAO();


            try {
                // Cleanup previous tests
                dao.deleteBruger(Kasper);
                dao.deleteBruger(Hans);
                dao.deleteBruger(Stig);


                // Add users
                dao.createBruger(Kasper);
                dao.createBruger(Hans);
                dao.createBruger(Stig);

                // Test getBruger
                BrugerDTO hej = dao.getBruger(1);
                System.out.println(hej.toString());

                // Test getBrugerList
                List<BrugerDTO> brugerDTOList;
                brugerDTOList = dao.getBrugerList();
                for (int i = 0; i < brugerDTOList.size(); i++)
                    System.out.println(brugerDTOList.get(i).toString());

                // Cleanup previous tests
                dao.deleteBruger(Kasper);
                dao.deleteBruger(Hans);
                dao.deleteBruger(Stig);

            } catch (Exception e) {
                e.printStackTrace();
            }
        }*/
    }

